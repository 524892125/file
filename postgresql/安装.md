二、使用 PostgreSQL 官方仓库安装（推荐）
1️⃣ 添加 PostgreSQL 官方 Yum 源
👉 CentOS 7
sudo yum install -y https://download.postgresql.org/pub/repos/yum/reporpms/EL-7-x86_64/pgdg-redhat-repo-latest.noarch.rpm

👉 CentOS 8 / Stream / Rocky / AlmaLinux
sudo dnf install -y https://download.postgresql.org/pub/repos/yum/reporpms/EL-8-x86_64/pgdg-redhat-repo-latest.noarch.rpm

👉 CentOS Stream 9 / Rocky 9 / AlmaLinux 9
sudo dnf install -y https://download.postgresql.org/pub/repos/yum/reporpms/EL-9-x86_64/pgdg-redhat-repo-latest.noarch.rpm

2️⃣ 禁用系统默认 PostgreSQL（防止冲突）
CentOS 7:
sudo yum -qy module disable postgresql

CentOS 8+:
sudo dnf -qy module disable postgresql

3️⃣ 安装 PostgreSQL（例如 16 版）
sudo yum install -y postgresql16 postgresql16-server


或（CentOS 8/9）：

sudo dnf install -y postgresql16 postgresql16-server

4️⃣ 初始化数据库
sudo /usr/pgsql-16/bin/postgresql-16-setup initdb

5️⃣ 设置开机启动并启动服务
sudo systemctl enable postgresql-16
sudo systemctl start postgresql-16

6️⃣ 查看运行状态
sudo systemctl status postgresql-16

🧠 三、基本使用命令
1️⃣ 切换到 postgres 用户
sudo su - postgres

2️⃣ 进入数据库命令行
psql

3️⃣ 修改 postgres 默认密码
ALTER USER postgres WITH PASSWORD 'your_password';

4️⃣ 退出
\q

🧱 四、允许远程连接（可选）

编辑配置文件：

sudo vi /var/lib/pgsql/16/data/postgresql.conf


找到：

#listen_addresses = 'localhost'


改为：

listen_addresses = '*'


再编辑访问规则：

sudo vi /var/lib/pgsql/16/data/pg_hba.conf


添加一行（允许外部连接）：

host all all 0.0.0.0/0 md5


重启服务：

sudo systemctl restart postgresql-16

✅ 五、验证版本
psql --version


输出类似：

psql (PostgreSQL) 16.2


是否希望我帮你写一份 CentOS 一键安装 PostgreSQL 16 的脚




sudo systemctl restart postgresql-15