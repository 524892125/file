```html
<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title></title>
</head>
<body>

  <script>
    // 加密：text -> base64
function xorEncryptBase64(text, key) {
  const t = new TextEncoder().encode(text);
  const k = new TextEncoder().encode(key);
  const out = new Uint8Array(t.length);
  for (let i = 0; i < t.length; i++) {
    out[i] = t[i] ^ k[i % k.length];
  }
  // 转 base64 方便网络传输
  return btoa(String.fromCharCode(...out));
}
// 解密：base64 -> text
function xorDecryptBase64(b64, key) {
  const raw = atob(b64);
  const bytes = Uint8Array.from(raw, c => c.charCodeAt(0));
  const k = new TextEncoder().encode(key);
  const out = new Uint8Array(bytes.length);
  for (let i = 0; i < bytes.length; i++) {
    out[i] = bytes[i] ^ k[i % k.length];
  }
  return new TextDecoder().decode(out);
}

var enc = "123123213123213123142398723729";
  var a = xorEncryptBase64(enc, "sdfsdfsdfsdfsfsdfdsf");
  var dec = xorDecryptBase64(a, "sdfsdfsdfsdfsfsdfdsf");

  console.log("a", a)

  console.log("dec", dec)
  </script>

</body>
</html>
```